#include "MetroFileManager.h"

#define FILEPATH "DTHC.txt"  // 存储至文件的路径在这里设置，默认为DTHC.txt


bool MetroFileManager::SaveMetroSystem(const MetroSystem &system, const std::string &fileName) {

}

bool MetroFileManager::LoadMetroSystem(MetroSystem &system, const std::string &fileName) {

}